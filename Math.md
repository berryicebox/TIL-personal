## 1️⃣ Math.min(...)
- 전달된 숫자들 중 가장 작은 값을 반환하는 메서드

### 🔹 기본 문법
```
Math.min(value1, value2, ...)
```

### 🔹 사용 예시
```
console.log(Math.min(3, 7, 2)); // 2
console.log(Math.min(10, -1));  // -1

move = Math.min(move, first, second);
```
