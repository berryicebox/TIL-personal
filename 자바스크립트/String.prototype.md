## 1️⃣ String.prototype.repeat()
- 문자열을 지정된 횟수만큼 반복하여 새로운 문자열을 반환하는 메서드다.
- 숫자의 자릿수를 늘리거나(5 -> 55 -> 555) 특정 패턴을 만들 때 유용하다.

### 🔹 기본 문법
```JavaScript
string.repeat(count)
```

### 🔹 사용 예시
```JavaScript
const N = "5";

console.log(N.repeat(3)); // "555"
console.log(Number(N.repeat(2))); // 55 (숫자로 변환)
``` 

## 2️⃣ String.prototype.charCodeAt()
- 문자열에서 특정 위치의 문자를 유니코드(UTF-16) 값으로 변환하는 메서드
- 영문 알파벳과 숫자는 ASCII 영역에 포함되기 때문에 아스키코드처럼 사용해도 무방하다.

### 🔹 기본 문법
```javascript
string.charCodeAt(index)
```
- index : 유니코드 값을 구할 문자 위치

### 🔹 사용 예시
```javascript
const ch = 'A';

console.log(ch.charCodeAt(0)); // 65
console.log('Z'.charCodeAt(0)); // 90
console.log('a'.charCodeAt(0)); // 97
```

### 🔹 주의사항
- 반환값은 Number
- 문자열이 비어있거나 인덱스가 범위를 벗어나면 NaN
```javascript
''.charCodeAt(0); // NaN
```
