## Array.from()
- 유사 배열 객체나 반복 가능한 객체를 얕게 복사하여 새로운 배열을 만든다.
- 두 번째 인자로 콜백 함수를 전달해 배열을 생성함과 동시에 값을 초기화할 수 있다.
- 특정 길이를 가진 배열을 만들고 싶을 때 자주 사용된다.

### 🔹 기본 문법
```JavaScript
Array.from(arrayLike, mapFn)
```

### 🔹 사용 예시
```JavaScript
// 길이가 5인 배열을 만들고 모든 요소를 Set으로 채움
const setArray = Array.from({ length: 5 }, () => new Set());

// 인덱스를 활용한 배열 생성
const numbers = Array.from({ length: 3 }, (v, i) => i + 1);
console.log(numbers); // [1, 2, 3]
```
