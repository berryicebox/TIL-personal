## Array (배열)
- 자바스크립트에서 가장 기본이 되는 순서가 있는 리스트입니다.

```
Array.from(obj, callback):
```
- 역할: 유사 배열 객체나 반복 가능한 객체(예: Set, Map, String)를 새로운 배열로 변환합니다.
- 특징: 두 번째 인자로 콜백 함수를 넣어 생성과 동시에 값을 초기화할 수 있습니다.

```
Array.from({ length: 3 }, (_, i) => i); // [0, 1, 2]
```
